public class Penghitung {
    public int modus(){
        Kelas kls = new Kelas();
        Integer modus;
        modus = Integer.valueOf(kls.getNilai1() + kls.getNilai2()+ kls.getNilai3());
        return 0;
    }
}
